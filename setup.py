import os
import subprocess
# TODO 
# automate venv creation and installation of required python libraries
# venv_python = os.path.join('venv', 'bin', 'python')
# a = subprocess.run('ls', capture_output=True).stdout.decode()
# print(a.splitlines())
# print(a.splitlines().count('venv'))
# if a.splitlines().count('venv') == 0:
#     print("Making venv:\n")
#     subprocess.run(['python', '-m', 'venv', 'venv'])
# subprocess.run([venv_python,'pip', 'list'])
# subprocess.run(['pip', 'install', 'requirements.txt'])